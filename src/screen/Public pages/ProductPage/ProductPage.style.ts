import { Grid } from "@mui/material";
import { styled } from "@mui/material/styles";

export const DitailsBox = styled(Grid)({
 flexWrap: "nowrap",
  justifyContent: "space-between",
  gap: "20px",
  marginBottom: "25px",
});

export const ImgBox = styled(Grid)({
  WebkitPaddingEnd: "40px",
});
