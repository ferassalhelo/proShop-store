import { Box, Typography } from "@mui/material";
import { styled } from "@mui/material/styles";

export const ProductName = styled(Typography)({});

export const DeleteBotton = styled(Box)({
  position: "absolute",
  top: "20px",
  right: "20px",
  cursor: "pointer",
  fontSize: "28px",
});
